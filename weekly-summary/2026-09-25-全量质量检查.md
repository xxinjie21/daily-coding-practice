# daily-coding-practice 全量质量检查报告

- 检查日期：2026-09-25
- 检查范围：`daily-task/` 下全部 **39 个** 题解目录（2026-07-21 ~ 2026-09-25）
- 检查对象：每个目录的 `题解.md` + `code/Demo.java`
- 检查维度：①可读性 ②技术正确性 ③是否符合生成规范（结构 / 篇幅 / 代码风格 / 编译可运行）

## 一、总体结论

**整体质量良好，无严重技术错误。** 39 份 Demo 全部可编译、可运行；内容未发现跑题或概念性错误。主要偏差集中在 **标题风格不统一**、**个别篇幅超标** 和 **少量表述不一致**，都属于"打磨"级别，不影响使用。

| 维度 | 结果 |
|---|---|
| 目录结构（恰好 2 个文件：题解.md + code/Demo.java） | 39 / 39 通过 |
| Demo.java 编译（`javac -encoding UTF-8 --release 17`） | 39 / 39 通过（Java 17 兼容性无问题） |
| Demo.java 运行（退出码 0、无异常） | 39 / 39 通过 |
| 题目匹配（题解是否真在回答该题） | 39 / 39 通过，无跑题 |
| 可读性（生活比喻、名词解释、面向新手） | 整体达标，无"显然/众所周知"类措辞 |
| 代码是否按原题思路实现（非自创解法） | 39 / 39 通过 |
| 标题规范（归纳式短标题） | **28 / 39 通过，11 个不合格** |
| 编译注释含 `-encoding UTF-8` | **37 / 39 通过，2 个不合格** |
| 篇幅适中（题解 ≲120 行、Demo ≲450 行） | **30 / 39 通过，9 个超标** |

## 二、需要修的问题

### 问题 1：首行标题照抄原题、保留题号（11 个）—— 最集中

规范要求首行是"点出核心主题的归纳式短标题"（参照 `# 负载均衡器：怎样把请求均匀分给多台服务器`）。以下 11 个目录直接沿用了原题原文：

| 目录 | 当前首行 | 建议改为 |
|---|---|---|
| 2026-08-14-分布式锁取舍 | `# 35. 你说你项目上用了分布式锁，加锁后并发度不就降低了吗？` | `# 分布式锁取舍：加了锁并发度一定下降吗` |
| 2026-08-15-SSO认证中心容灾 | `# SSO 系统中，如果认证中心挂了怎么办？…` | `# SSO 认证中心容灾：认证中心挂了怎么不掉线` |
| 2026-08-19-游标分页防丢数据 | `# 使用 LIMIT OFFSET 分页同步时，发现数据丢失了…` | `# 游标分页：为什么 OFFSET 翻页会丢数据` |
| 2026-08-20-手写HashMap | `# 让你设计一个 HashMap，怎么设计？` | `# 手写 HashMap：数组+链表怎么设计` |
| **2026-08-22-外部排序** | `33. 如果现在有 500G 数据需要排序…` ← **连 `#` 都缺** | `# 外部排序：内存装不下 500G 怎么排` |
| 2026-08-24-数据同步容错机制 | `# 如何设计一个高可用的数据同步系统？…` | `# 数据同步容错：出错重试怎么不重复不丢数` |
| 2026-08-27-文件落盘过程 | `# 24. Java 写入文件到磁盘会经历哪些过程？` | `# 文件落盘：Java 写文件到底走了哪些路` |
| 2026-08-30-Excel导出优化 | `# 10. 项目上有个导出 excel 场景发现很慢，怎么优化？` | `# Excel 导出优化：几百万行怎么不 OOM 不卡死` |
| 2026-09-01-缓存降级容灾 | `# 项目用了 Redis 缓存来提升并发度，假设 Redis 挂了怎么办？` | `# 缓存降级容灾：Redis 挂了怎么不雪崩` |
| 2026-09-18-接口变慢排查 | `# 30. 接口变慢了应该如何排查？…` | `# 接口变慢排查：一条链路怎么找出慢在哪` |
| 2026-09-23-JVM内存与OOM排查 | `# 111. 怎么分析 JVM 当前的内存占用情况？OOM 后怎么分析？` | `# JVM 内存与 OOM：内存去哪了、OOM 后怎么查` |

> 注：`2026-08-22-外部排序` 首行缺 `#` 是唯一一处**格式错误**（不只是风格问题），会干扰"读首行标题做去重"的逻辑。

### 问题 2：Demo.java 头部编译命令缺 `-encoding UTF-8`（2 个）

规范明确要求"务必带 `-encoding UTF-8`，否则中文可能因环境编码波动而乱码/编译异常"。

| 目录 | 当前 | 应改为 |
|---|---|---|
| 2026-07-21-负载均衡器 | `javac --release 17 Demo.java && java Demo` | `javac -encoding UTF-8 --release 17 Demo.java && java Demo` |
| 2026-07-22-关联表vs冗余字段 | 同上 | 同上 |

### 问题 3：篇幅明显超标（9 个）

规范要求"简单明了、不要太长""Demo 不必过于复杂，不要堆砌成完整生产系统"。

| 目录 | 题解.md | Demo.java | 问题 |
|---|---|---|---|
| 2026-08-10-订单超时取消 | 186 行 | **722 行** | 最长。MQ + ZSET + Lua + 分布式锁整套搭出，偏离"聚焦小例子" |
| 2026-08-07-索引失效排查 | 184 行 | **672 行** | 手搓 mini SQL 引擎 + 慢日志 + digest，脚手架过重 |
| 2026-08-16-缓存预热 | **191 行** | 642 行 | 题解最长，实验与实测表可合并 |
| 2026-08-12-热销榜TopN | 123 行 | 565 行 | 实现了完整迷你 ZSET |
| 2026-08-02-滑动验证码 | 88 行 | 476 行 | 略超 |
| 2026-08-08-最大在线人数 | 133 行 | 474 行 | 略超 |
| 2026-08-04-接口调用量统计 | 131 行 | 464 行 | 略超 |
| 2026-08-03-单体是否拆微服务 | 124 行 | 343 行 | 题解略超 |
| 2026-09-18-接口变慢排查 | 126 行 | 483 行 | 题解略超 |

### 问题 4：内容表述不一致 / 小瑕疵（10 个）

| 严重度 | 目录 | 问题 |
|---|---|---|
| 中等 | 2026-07-27-支付削峰限流 | FIFO 校验中 `queue.poll()` 与顺序记录 `add()` 之间无同步，多 Worker 下可能误判，导致末尾断言偶发抛异常 |
| 中等 | 2026-07-23-敏感数据加密存储 | 题解说哈希结果是"64 位十六进制指纹"，Demo 实际返回 Base64，两处表述不一致 |
| 轻微 | 2026-07-26-抢红包金额算法 | 最后一人金额未兜底，倒数第二人取满上限时最后一人可能为 0 元，与注释"避免抢到 0 元"矛盾 |
| 轻微 | 2026-07-31-数据同步断点续传 | 小标题写"三、怎么做（四步）"实际列了 5 步；首个小标题缺"一、"编号 |
| 轻微 | 2026-08-11-大表在线加索引 | 正文说"直接 ALTER 锁表数小时"，补充又称"5.6+ 在线 DDL 不锁表"，前后略矛盾 |
| 轻微 | 2026-08-13-防重放攻击 | 称"SETNX + 设过期时间"这一步原子，实际需 `SET key 1 EX 360 NX` 才是原子操作 |
| 轻微 | 2026-08-27-文件落盘过程 | 三档对照表把"FileOutputStream 直接写"归入"用户态缓冲"，与实验二"无用户态缓冲"自相矛盾 |
| 轻微 | 2026-09-01-缓存降级容灾 | Demo 的 `LocalCache`/`FakeRedis` 用 `k`/`v`/`e` 单字母参数，违反"命名见名知意" |
| 轻微 | 2026-09-20-第三方接口异步化 | 注释称 `pollLast`"队列空时会阻塞"，实际用 `isEmpty` 非阻塞轮询，注释与实现不符 |
| 轻微 | 2026-08-07-索引失效排查 | 题解慢日志示例把 `phone LIKE ?` 记为 3 次调用，Demo 中实际跑 60 次 |

## 三、修复执行结果

### 已修复（2026-09-25，commit 08bd3c9）

| 档位 | 内容 | 状态 |
|---|---|---|
| P0 | `2026-08-22` 首行补 `#` 并改归纳式标题 | ✅ 已修 |
| P0 | `2026-07-21`、`2026-07-22` 编译命令补 `-encoding UTF-8` | ✅ 已修 |
| P1 | 11 个照抄原题的标题统一改为「主题：一句话」式短标题 | ✅ 已修 |
| P2 | `2026-07-27` FIFO 校验并发竞态（取单与登记顺序加锁） | ✅ 已修，连跑 4 次校验通过 |
| P2 | `2026-07-26` 红包最后一人可能为 0 元（最后一轮预留 1 分） | ✅ 已修 |
| P2 | `2026-07-31` 首节补「一、」编号、"四步"改为"四步 + 一步可选" | ✅ 已修 |
| P2 | `2026-08-07` 慢日志 `calls` 口径说明（进慢日志次数 ≠ 总调用次数） | ✅ 已修 |
| P2 | `2026-08-11` ALTER 锁表表述与"5.6+ 在线 DDL"前后呼应 | ✅ 已修 |
| P2 | `2026-08-13` `SETNX`+`EXPIRE` 改为原子写法 `SET ... NX EX`（含流程图） | ✅ 已修 |
| P2 | `2026-08-27` 对照表「FileOutputStream 无用户态缓冲」自相矛盾 | ✅ 已修 |
| P2 | `2026-09-01` Demo 单字母参数 `k`/`v`/`e` 改为 `key`/`value`/`entry` | ✅ 已修 |
| P2 | `2026-09-20` `pollLast` 注释与实现不符（非阻塞 vs 阻塞） | ✅ 已修 |

修复后已对 6 个被改动的 `Demo.java` 重新编译（`-encoding UTF-8 --release 17`）并运行，全部退出码 0、无异常；编译产物已清理。

### 未修复（待后续处理）

| 项 | 原因 |
|---|---|
| `2026-07-23-敏感数据加密存储`：题解"64 位十六进制指纹"与 Demo 的 Base64 表述不一致 | 读取该文件时**用户拒绝共享敏感内容**，本次无法查看或修改该文件，留待人工处理 |
| P3：9 个超长文件精简（`08-10` 722 行、`08-07` 672 行、`08-16` 642 行等） | 改动大、涉及内容重写，建议单独排期逐步收敛 |

## 四、复现方式（本次检查用的命令）

```bash
# 1) 结构 + 行数
for d in daily-task/*/; do echo "$d $(ls $d | wc -l) $(wc -l < $d/题解.md) $(wc -l < $d/code/Demo.java)"; done

# 2) 全量编译（--release 17 同时校验 Java 17 兼容性）
cd daily-task/<目录>/code && javac -encoding UTF-8 --release 17 Demo.java

# 3) 全量运行
java Demo; echo $?

# 4) 清理编译产物（务必执行，勿提交 .class）
find daily-task -name '*.class' -delete
```
